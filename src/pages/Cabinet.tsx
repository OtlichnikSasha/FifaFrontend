import React, {FC, useEffect} from 'react';

export const Cabinet: FC = () => {
    useEffect(() => {
        document.title = "Личный кабинет"
    }, [])
    return (
        <div>

        </div>
    );
};

