import React, {FC, useEffect} from 'react';

export const AuthRegistrationPage: FC = () => {
    useEffect(() => {
        document.title = "Регистрация"
    }, [])
    return (
        <div>

        </div>
    );
};

